user + password "Robin","flaskportfolioapp"

install gunicorn with pip:

	pip install gunicorn

Create file :
	 requirements.txt

Add spacification to the requirements.txt file :

	pip freeze > requirements.txt

create Procfile with command cmd , the first name must the name of you App.py file , notice the names cannot be the same:

	echo "web: gunicorn App:app" > Procfile

commit changes in repository
	
	git add *
	git commit -m "comment etc"

create heroku app running in cmd :

	heroku create app_name

create the right build packs / first clearing all buildpacks, then adding python:

	heroku buildpacks:clear

	heroku buildpacks:add heroku/python

push files:
	git push heroku main



execute with heroku open


* __init__.py files will cause issues
*heroku doesnt allow writting files inside ist environment therefore a real database mus be used